{{ define "installers-script" }}
echo "#####Running provider script-installers#####"
{{ range .packages.script_installers -}}
{{- if and (has $.chezmoi.os .os) (or (not (hasKey . "computer")) (and (hasKey $ "computer") (has $.computer .computer))) }}
if ! {{ .check }} &> /dev/null; then
    echo "Installing {{ .name }}..."
{{ .install | trim | indent 4 }}
else
    echo "{{ .name }} is already installed!"
fi
{{- if hasKey . "post_install" }}
{{ .post_install }}
{{- end }}
{{ end -}}
{{- end }}
{{ end }}
