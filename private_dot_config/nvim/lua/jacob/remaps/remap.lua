-- Insert Mode
vim.cmd("inoremap jj <Esc>")
-- Make it so the cursors stays center when you page down
vim.cmd("inoremap <c-d>  <c-d zz>")
-- Make it so the cursors stays center when you page up
vim.cmd("inoremap <c-u>  <c-d zz>")

-- Normal Mode
